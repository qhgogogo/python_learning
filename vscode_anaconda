%%vscode
in Properties/Compatiility/change settings for all users/run as administrator

%%in anaconda create environment
python --version
conda create --name myenv python=3.xxx  % version can change
conda activate myenv 
%then install the packages in this env

conda install ********

%% in vs code, select this env 
To activate the Anaconda environment in VSCode, you need to select it as your Python interpreter.
Open the Command Palette (Ctrl+Shift+P), type ‘Python: Select Interpreter’, and hit Enter.
A list of available interpreters will appear. Select the one that corresponds to your Anaconda environment.
